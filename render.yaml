services:
  - type: web
    name: library-api
    plan: free
    branch: main
    buildCommand: mvn clean install -DskipTests
    startCommand: java -jar target/nada-0.0.1-SNAPSHOT.jar
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: production

